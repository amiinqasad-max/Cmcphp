# Pairs with cmcphp-scheduler.service.example — fires it every minute,
# same cadence as the cron entry it replaces.

[Unit]
Description=Run CMCPHP scheduler tick every minute

[Timer]
OnCalendar=*-*-* *:*:00
AccuracySec=1sec
Persistent=true

[Install]
WantedBy=timers.target
